	public static  Node insert(Node head,int data)
	{
        Node nen=new Node(data);
  	if(head==null)
        head=nen;
        else
        {
         Node temp=head;
            while(temp.next!=null)
                temp=temp.next;
            temp.next=nen;
        }
        return head;
	}